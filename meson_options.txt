option('vectorization', type: 'boolean', value: true, description: 'Enable auto vectorization for GCC')
option(
  'capture-backend',
  type: 'combo',
  choices: ['pipewire', 'pulseaudio'],
  value: 'pulseaudio',
  description: 'Audio capture backend. Use pipewire or pulseaudio (default) as a fallback for systems where PipeWire stream autoconnect does not work.',
)
